"use strict";
(self["webpackChunkmanagement"] = self["webpackChunkmanagement"] || []).push([["resources_bundle_js-webpack_sharing_consume_default_jquery_jquery"],{

/***/ "./resources/bundle.js":
/*!*****************************!*\
  !*** ./resources/bundle.js ***!
  \*****************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony import */ var _patternslib_patternslib_src_core_registry__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @patternslib/patternslib/src/core/registry */ "./node_modules/@patternslib/patternslib/src/core/registry.js");
/* harmony import */ var _pat_tiles_management_tiles_management__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./pat-tiles-management/tiles-management */ "./resources/pat-tiles-management/tiles-management.js");


_patternslib_patternslib_src_core_registry__WEBPACK_IMPORTED_MODULE_0__["default"].init();

/***/ }),

/***/ "./resources/pat-tiles-management/tiles-management.js":
/*!************************************************************!*\
  !*** ./resources/pat-tiles-management/tiles-management.js ***!
  \************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   Pattern: () => (/* binding */ Pattern),
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__),
/* harmony export */   parser: () => (/* binding */ parser)
/* harmony export */ });
/* harmony import */ var _patternslib_patternslib_src_core_basepattern__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @patternslib/patternslib/src/core/basepattern */ "./node_modules/@patternslib/patternslib/src/core/basepattern.js");
/* harmony import */ var _patternslib_patternslib_src_core_parser__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! @patternslib/patternslib/src/core/parser */ "./node_modules/@patternslib/patternslib/src/core/parser.js");
/* harmony import */ var _patternslib_patternslib_src_core_registry__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! @patternslib/patternslib/src/core/registry */ "./node_modules/@patternslib/patternslib/src/core/registry.js");
/* harmony import */ var sortablejs__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! sortablejs */ "webpack/sharing/consume/default/sortablejs/sortablejs");
/* harmony import */ var sortablejs__WEBPACK_IMPORTED_MODULE_3___default = /*#__PURE__*/__webpack_require__.n(sortablejs__WEBPACK_IMPORTED_MODULE_3__);
// src/patterns/tiles-management/tiles-management.js





const parser = new _patternslib_patternslib_src_core_parser__WEBPACK_IMPORTED_MODULE_1__["default"]("tiles-management");
parser.addArgument("managerId", null);
class Pattern extends _patternslib_patternslib_src_core_basepattern__WEBPACK_IMPORTED_MODULE_0__["default"] {
  static name = "tiles-management";
  static trigger = ".pat-tiles-management";
  static parser = parser;
  async init() {
    __webpack_require__.e(/*! import() */ "resources_pat-tiles-management_tiles-management_scss").then(__webpack_require__.bind(__webpack_require__, /*! ./tiles-management.scss */ "./resources/pat-tiles-management/tiles-management.scss"));

    // Patternslib converte 'manager-id' da HTML/parser in 'managerId' qui.
    this.managerId = this.options.managerId;
    if (!this.managerId) {
      this.el.innerHTML = `<p class="error">Errore: l'attributo 'data-pat-tiles-management' deve specificare un manager-id.</p>`;
      console.error("manager-id non fornito per pat-tiles-management", this.el);
      return;
    }
    await this._loadManager();
  }
  async _loadManager() {
    const portalUrl = document.body.dataset.portalUrl || "";
    this.el.innerHTML = '<div class="spinner-border" role="status"><span class="visually-hidden">Loading...</span></div>';
    const baseUrl = document.body.dataset.baseUrl || "";
    const url = new URL(`${baseUrl}/tiles_management`);
    url.searchParams.append("managerId", this.managerId);
    url.searchParams.append("ajax_load", "1");
    try {
      const response = await fetch(url);
      if (!response.ok) {
        throw new Error(`Errore di rete: ${response.statusText}`);
      }
      const html = await response.text();
      if (!html.trim()) {
        this.el.remove();
        return;
      }
      this.el.innerHTML = html;
      this.el.dispatchEvent(new CustomEvent("rtTilesLoaded", {
        bubbles: true
      }));
      this._enableInteractions();
      _patternslib_patternslib_src_core_registry__WEBPACK_IMPORTED_MODULE_2__["default"].scan(this.el);
    } catch (err) {
      console.error("Impossibile caricare il gestore dei tile:", err);
      this.el.innerHTML = `<p class="error-manager">Si è verificato un errore durante il caricamento.</p>`;
    }
  }
  _enableInteractions() {
    const addTileButton = this.el.querySelector(".add-tile-btn");
    if (addTileButton) {
      addTileButton.addEventListener("click", e => {
        e.preventDefault();
        this._initializeAddButton(addTileButton, e.currentTarget.href);
      });
    }
    this.el.querySelectorAll(".tilesList .tileWrapper").forEach(tile => {
      this._enableTileHover(tile);
      this._enableAjaxLinks(tile);
    });
    if (window.innerWidth > 991) {
      this._enableSorting();
    }
  }
  async _initializeAddButton(button, url) {
    let container = button.nextElementSibling;
    if (!container || !container.classList.contains("available-tiles")) {
      button.parentElement.insertAdjacentHTML("beforeend", '<div class="available-tiles" style="display: none;"></div>');
      container = button.nextElementSibling;
      try {
        const response = await fetch(url);
        const text = await response.text();
        const tempDiv = document.createElement("div");
        tempDiv.innerHTML = text;
        const tilesList = tempDiv.querySelector("#content .list-group");
        if (tilesList) {
          // FIX 2: Usa appendChild per includere il div.list-group wrapper.
          container.innerHTML = ""; // Pulisci prima il contenitore
          container.appendChild(tilesList);
          container.querySelectorAll("a.list-group-item").forEach(link => {
            const modalOptions = {
              actionOptions: {
                redirectOnResponse: true,
                redirectToUrl: window.location.href,
                data: {
                  managerId: this.managerId
                }
              },
              templateOptions: {
                className: "modal fade tiles-management-modal"
              }
            };
            link.dataset.patModal = JSON.stringify(modalOptions);
          });
          _patternslib_patternslib_src_core_registry__WEBPACK_IMPORTED_MODULE_2__["default"].scan(container);
        }
        container.style.display = "block";
      } catch (err) {
        console.error("Errore nel caricamento dei tile disponibili:", err);
      }
    } else {
      container.style.display = container.style.display === "none" ? "block" : "none";
    }
  }
  _enableAjaxLinks(tile) {
    const links = tile.querySelectorAll("div.tileEditButtons a.tileVisibilityLink, div.tileEditButtons .tileSizeLink a.dropdown-item");
    links.forEach(link => {
      link.addEventListener("click", async e => {
        e.preventDefault();
        const url = new URL(e.currentTarget.href);
        url.searchParams.append("managerId", this.managerId);
        url.searchParams.append("ajax_load", "1");
        try {
          await fetch(url);
          await this._loadManager();
        } catch (err) {
          console.error("Errore durante l'aggiornamento del tile:", err);
        }
      });
    });
  }
  _enableTileHover(tile) {
    const editButtons = tile.querySelector(".tileEditButtons");
    if (editButtons) {
      editButtons.style.display = "none";
      tile.addEventListener("mouseenter", () => {
        tile.classList.add("editableTile");
        editButtons.style.display = "block";
      });
      tile.addEventListener("mouseleave", () => {
        tile.classList.remove("editableTile");
        editButtons.style.display = "none";
      });
    }
  }
  _enableSorting() {
    sortablejs__WEBPACK_IMPORTED_MODULE_3___default().create(this.el.querySelector(".tilesList"), {
      draggable: "div.tileWrapper",
      animation: 200,
      onUpdate: async data => {
        const tileIds = Array.from(data.to.children).map(el => el.dataset.tileid);
        const absoluteUrl = document.body.dataset.baseUrl || "";
        const url = new URL(`${absoluteUrl}/reorder_tiles`);
        url.searchParams.append("managerId", this.managerId);
        url.searchParams.append("ajax_load", "1");
        url.searchParams.append("tileIds", JSON.stringify(tileIds));
        try {
          const response = await fetch(url);
          if (response.status === 204) {
            return;
          }
          if (!response.ok) {
            const errorData = await response.json().catch(() => null);
            const errorMessage = errorData?.message || `Errore HTTP: ${response.status}`;
            throw new Error(errorMessage);
          }
          const data = await response.json();
          if (data && data.message) {
            console.error(data.message);
          }
        } catch (err) {
          console.error("Errore durante il riordino dei tile:", err);
        }
      }
    });
  }
}
_patternslib_patternslib_src_core_registry__WEBPACK_IMPORTED_MODULE_2__["default"].register(Pattern);
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (Pattern);


/***/ })

}]);
//# sourceMappingURL=resources_bundle_js-webpack_sharing_consume_default_jquery_jquery.9397662ad5403f8b83dc.min.js.map